import { ComponentProps } from "react";
import { NavBar } from "./NavBar";

const CustomNavBar = ({
  children,
  links,
  title,
}: ComponentProps<typeof NavBar>) => {
  return <div>CustomNavBar</div>;
};
