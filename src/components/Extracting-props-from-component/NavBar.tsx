export const NavBar = (props: {
  title: string;
  links: string[];
  children: React.ReactNode;
}) => {
  return <div>Some content</div>;
};

//? 3rd-party lib. => internally types => cannot be exported, imported.
